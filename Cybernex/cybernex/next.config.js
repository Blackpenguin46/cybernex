/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // Enables the app directory for Next.js 13+
  },
};

module.exports = nextConfig;
